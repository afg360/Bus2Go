<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    
    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/configureWelcomeTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/welcome"
        android:textSize="55sp"
        android:textAlignment="center"
        android:layout_marginTop="100dp"
        />
    
    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/configureWelcomeDescriptionTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="We will setup some configurations first before starting"
        android:textAlignment="center"
        android:textSize="30sp"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="30dp"
        android:layout_marginHorizontal="25dp"
        />
    
    <com.google.android.material.button.MaterialButton
        android:id="@+id/configureContinueButton"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:text="@string/config_continue"
        android:textSize="20sp"
        android:textColor="@color/light"
        android:layout_marginTop="40dp"
        />

</LinearLayout>